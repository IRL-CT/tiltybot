/*
 * TiltyBot — Tilty Mode
 * Pan/tilt head controlled by phone gyroscope over WebSocket.
 * Flash with: pio run -e tilty -t upload
 * TODO: implement
 */
#include <Arduino.h>
void setup() { Serial.begin(115200); delay(2000); Serial.println("Tilty mode — not yet implemented"); }
void loop() { delay(1000); }
